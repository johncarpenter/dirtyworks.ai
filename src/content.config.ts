/* This site uses no content collections. Page copy lives in typed modules under src/content/
   and is imported directly by section components, which keeps it type-checked and greppable.

   Declaring an empty collection set stops Astro from auto-generating a deprecated `pages`
   collection from those modules and from warning that it found no markdown in them. */
export const collections = {};
